<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_peerpal"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_peerpal_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PeerPalApp"
        tools:targetApi="31">
        <activity
            android:name=".ui.messages.ChatActivity"
            android:exported="true" />
        <activity
            android:name=".ui.messages.MessagesFragment"
            android:exported="true" />
        <activity
            android:name=".profileCreation"
            android:exported="true"></activity>
        <activity
            android:name=".splashScreen"
            android:exported="true"
            android:label="@string/title_activity_main">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".signUpActivity"
            android:exported="true"></activity>
        <activity
            android:name=".MainActivity"
            android:exported="true"></activity>
        <activity
            android:name=".Login"
            android:exported="true"></activity>
    </application>

</manifest>